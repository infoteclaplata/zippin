<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="choose_delivery_carrier_view_form" model="ir.ui.view">
        <field name="name">choose.delivery.carrier.zippin.form</field>
        <field name="model">choose.delivery.carrier</field>
        <field name="inherit_id" ref="delivery.choose_delivery_carrier_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='carrier_id']" position="after">
                <field name="zippin_pickup_view_invisible" invisible="1"/>
                <field name="zippin_pickup_view_id" invisible="1"/>
                <field name="zippin_pickup" attrs="{'invisible':[('zippin_pickup_view_invisible', '=', True)]}"
                    domain="[('carrier_id','=', zippin_pickup_view_id)]"/>
            </xpath>
        </field>
    </record>
</odoo>